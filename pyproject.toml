[project]
name = "linear-ticket-printer"
version = "0.1.0"
description = "Print out linear tickets on a thermal printer."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.32.0",
  "python-dotenv>=1.0.0",
]
