[project]
name = "papercut"
version = "0.1.0"
description = "Print your tickets on a real receipt printer"
readme = "README.md"
authors = [{ name = "KaBanks", email = "hi@krabby.dev" }]
license = "MIT"
requires-python = ">=3.14"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.32.0",
  "python-dotenv>=1.0.0",
  "python-escpos>=3.0",
]
